<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.JumPeak" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.JumPeak" parent="Base.Theme.JumPeak" />

    <style name="App.CustomIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/light_black_smoke</item>
    </style>
</resources>